# HANDOFF — Скрипт звонка в CRM + обучение менеджеров

> Передача работы из сессии, открытой в папке сайта (`C:\Clodecode\locksmitch`), в сессию в папке CRM.
> Дата: 2026-09-18. Ветка: **`claude/call-script-panel`** (от `origin/main` @ `d8a580f`).
> Этот файл самодостаточен: память прошлой сессии в CRM-проекте не видна — всё нужное здесь.

---

## 0. Как начать в новой сессии (первые 2 минуты)

1. `git fetch origin`
2. Забрать работу: `git merge claude/call-script-panel` в свою ветку (или работать прямо в старом worktree `C:\Clodecode\CRM\.claude\worktrees\call-script-panel`, если сессия там открыта).
   ⚠️ Локальный `main` в `C:\Clodecode\CRM` **отстаёт от origin на 75 коммитов** и содержит **чужие незакоммиченные правки** (priceBook.ts, settingsStore.ts, JobWizard.tsx, payments.js) — его **не трогать**, работать от `origin/main`.
3. `npm ci` (если нет node_modules), затем `npx vitest run callScripts.test.ts scorecard.test.ts` — всё зелёное.
4. Прочитать этот файл + `training/02-call-scripts.md` (тексты) + `callScripts.ts` (те же тексты в коде).

---

## 1. Задача и что уже решено владельцем

Владелец (Sultan, TrustKey Locksmith, Mesa AZ) нанимает **менеджеров на входящие звонки**. Менеджеры **русскоязычные, английский отличный**, опыта в локсмите и продажах — ноль. Учим **только менеджеров** (техников — нет). Ночных смен у менеджеров, скорее всего, нет.

**Решение по формату (владелец одобрил макет):** скрипт живёт **прямо в CRM**, на экране **New Job Intake** (`components/JobWizard.tsx`): слева форма заявки как сейчас, **справа панель скрипта**. Менеджер выбирает услугу (шаблон) → справа открывается скрипт этой услуги: шаги разговора (фраза EN + подсказка RU), цена автоматически из прайса CRM (день/ночь), кнопки «Клиент говорит…» с готовыми ответами. Скрипт можно переключить вручную.

**Факты от владельца (использовать в скриптах):**
- Свободный техник и время приезда — видно в CRM (TechPicker «who's closest»).
- Техники **требуют ID + регистрацию** машины (для дома ID — формальность).
- Менеджер **не даёт скидок**. Скидка $20 — только при заявке через форму на trustkeyaz.com.
- Звонки в OpenPhone **записываются**.
- **Брелоки, купленные клиентом, НЕ программируем** (часто не та модель/частота или уже прописаны в другую машину) — привозим свой ключ.
- **Запасной ключ** к машине = цена этого типа ключа (transponder from $149 / fob from $199 / smart from $279).
- **Клиент вне наших городов:** спросить срочность; если ближайший техник доедет ≤ ~30 минут — едем.
- **Чек** клиенту — по СМС или на почту, как удобно.
- Испанский не нужен.

**Вопросы к владельцу — ответы 2026-09-18:**
1. Простой ключ без чипа — **$149**, цена точная (`r-car-key`).
2. Извлечение сломанного ключа — **$169** (`r-key-extraction`, скрипт и шаблон «Broken key»).
3. «How long in business?» — **только правда**: «TrustKey opened in June 2026» (~3 месяца). Фраза про «опыт в других штатах» убрана, её никто не подтверждал.
4. Bonded — **нет** (владелец, 2026-09-18). Ответ в скрипте: «No, we're not bonded. We're a registered Arizona company and we carry liability insurance…».
5. Ночь — **+$60 к любому заказу после 8PM, и всё**. Отдельных ночных цен нет. В коде это `nightPriceOf()` в `priceBook.ts`, так считают скрипт, карточка заказа и ИИ (он считал ночь с 9PM, исправлено на 8PM).

**Процесс:** владелец сказал «сначала всё сделать и протестировать, запускать в конце», по кусочкам не выкладывать.

---

## 2. Что уже сделано (в этой ветке, закоммичено)

| Файл | Что |
|---|---|
| `training/01-knowledge-base.md` | База знаний «Локсмит с нуля» (RU): кто мы, типы автоключей, rekey vs замена, коммерция, все цены, словарь «как говорит клиент → что это», самопроверка |
| `training/02-call-scripts.md` | Скрипты (v0.2): 10 шагов звонка, 5 формул (цена / стресс / конкретика / имя / прощание), как звучать и холд, скрипты по услугам, ~20 возражений с вопросом вперёд, особые ситуации, СМС-шаблоны, причины незаписи, разбор звонка по 12 пунктам |
| `training/03-never-say.md` | Запретные фразы → что говорить вместо (основано на правилах честности после бана Google-профиля за Misrepresentation) |
| `training/research/01-phone-booking-research.md` | Исследование: booking rate, структура звонка, цена по телефону, стресс, закрытие, обучение, scorecard (~50 источников) |
| `training/research/02-locksmith-domain-and-az-law.md` | Исследование: советы FTC/BBB/ALOA, лицензии AZ, запись звонков, ID, брелоки, европейские машины |
| `callScripts.ts` | **Данные скриптов в коде** + резолвер токенов. 9 скриптов (`opening`, `car-lockout`, `car-key`, `akl`, `home-lockout`, `rekey`, `commercial`, `safe`, `lock-install`), 20 возражений, `NEVER_SAY`, `EMERGENCY_911`, `ratePrice()`, `resolveScript()`, `scriptText()`, `isNightInArizona()` |
| `callScripts.test.ts` | 11 тестов: цены день/ночь, +$60 при отсутствии nightPrice, фолбэк, токены, запрет «licensed / 1-year / 15 minutes», ночь по времени Аризоны |

**Токены в текстах:** `{me}` — имя менеджера; `{price:<rateId>}` — цена из прайса CRM (`useSettingsStore(s => s.priceBook)`), ночью `nightPrice ?? price + 60`; если ставки нет в прайсе — фолбэк из `RATE` в `callScripts.ts`; `[слот]` — менеджер заполняет вслух (подсвечивать).

---

## 3. Следующие шаги (порядок)

> **Статус 2026-09-18 (вторая сессия):** шаги A, B и C сделаны в ветке `claude/call-script-panel-2047f3`: `82433fc` (панель + встраивание, B.1–B.5) и `e4366c3` (B.6, порядок шагов визарда, отдельным коммитом: не одобрит → `git revert e4366c3`). **Шаг D ждёт одобрения владельца**, в main ничего не пушилось.
>
> Отличия от плана ниже:
> - Фраза 911 показывается **прямо в шаге «Безопасность»** (флаг `call911` в `ScriptStep`), потому что на ноутбуке общий блок 911 под шагами уходил за экран. Общий красный блок показывается на остальных шагах.
> - День/ночь идёт по часам Аризоны (пересчёт раз в минуту), пока менеджер не переключит вручную. Ручной выбор держится до закрытия визарда.
> - Панель живёт в двух экземплярах (колонка на десктопе и лист на телефоне). Оба не размонтируются при сворачивании/закрытии, чтобы менеджер не терял место в скрипте.
> - Высота колонки меряется `ResizeObserver` (область скролла − 80), а не `calc(100vh-…)`. Sticky в контейнере с `py-10` считает отступ от края контента, поэтому `top-0`.
> - Проверено: vitest 227/227, `tsc`, `npm run build`. Раскладка замерена на 1366×640, 1024×680, 1440×810, 1920×950 и 390×844. Техник панель не видит.
> - ⚠️ Встроенный браузер Claude, когда панель скрыта, не рисует кадры (`visibilityState: hidden`). ResizeObserver и rAF там молчат, и размеры врут. Замеры и скриншоты делались через headless Edge по CDP.

### Шаг A. `components/CallScriptPanel.tsx` ✅

Props: `scriptId: ScriptId`, `onScriptChange(id)`, `managerName: string`, `onCollapse?()`.
Внутри: `priceBook` из `useSettingsStore`, `night` = `useState(() => isNightInArizona())` + переключатель, `stepIdx`, выбранное возражение, раскрытие «Нельзя говорить». Сброс `stepIdx`/возражения при смене `scriptId`.

Разметка (тёмная тема как в визарде: `bg-slate-900`, `border-white/10`, `rounded-2xl/3xl`):
1. **Шапка:** иконка `ScrollText` + «Call script», `<select>` со всеми скриптами (`CALL_SCRIPTS[*].label`), кнопка День/Ночь (`Sun`/`Moon`, «Day rate» / «After 8PM»), кнопка свернуть (`PanelRightClose`, только на десктопе).
2. **Цены:** карточки по `script.rates`: название, крупно цена (`from $149` если `from`), мелко вторая цена (ночная/дневная). Цена через `ratePrice(ref, priceBook, night)`.
3. **Шаги:** список. Пройденные — строка с галочкой. Текущий — раскрыт: title, фраза EN (крупно, белым; цены — `text-emerald-300 font-bold`, слоты `[...]` — `text-amber-300 bg-amber-500/10 rounded px-1`), подсказка RU (`text-xs text-slate-400`), кнопка «Next». `alert: true` — янтарная/красная рамка. Клик по любому шагу — перейти.
4. **911** (если `script.emergency`): красный блок `Siren` + `EMERGENCY_911.say` + hint.
5. **«Клиент говорит…»:** чипы `OBJECTIONS[id].label` → под ними карточка ответа (EN + RU hint), повторный клик закрывает.
6. **«Нельзя говорить»:** сворачиваемый список `NEVER_SAY` (bad → good).

### Шаг B. Встроить в `components/JobWizard.tsx` ✅

1. **Шаблоны +2** (после Car Lockout): `car-key` («Car Key», иконка `Key`, cyan, lockType Automotive, complaint «Customer needs a new / spare car key made (has a working key).», priority **today**) и `akl` («All Keys Lost», иконка `KeySquare`, rose, Automotive, complaint «All car keys lost — new key made from scratch on-site.», priority **emergency**). Ввести интерфейс `JobTemplate` с необязательным `priority`; в `applyTemplate` — `tpl.priority ?? старая логика`. Tailwind-классы цветов писать литералами.
2. **Состояние скрипта:** `const [scriptId, setScriptId] = useState<ScriptId>('opening')`; `applyTemplate` → `setScriptId(tpl.id)` (id шаблонов = id скриптов). «Start from scratch» скрипт не меняет.
3. **Кому показывать:** `currentUser?.role === 'owner' || 'manager'`. Техникам — нет.
4. **Раскладка ≥ lg:** контейнер прокрутки `flex-1 overflow-y-auto px-6 py-10` → внутри `lg:flex lg:items-start lg:justify-center lg:gap-8`: слева форма `max-w-xl w-full` (при панели `lg:mx-0`), справа `<aside className="hidden lg:block w-[400px] xl:w-[440px] shrink-0 sticky top-0">` с панелью (`max-h-[calc(100vh-14rem)] overflow-y-auto`). Свёрнутое состояние — в `localStorage` `callScript.collapsed` (в try/catch); свёрнутая панель = маленькая кнопка «Script».
5. **Мобильный (< lg):** плавающая кнопка «Script» (`fixed right-4 bottom-28 lg:hidden`, выше футера) → нижний лист `fixed inset-0 z-[260] bg-black/60`, внутри панель `max-h-[85vh] overflow-y-auto rounded-t-3xl`.
6. **Порядок шагов визарда (предложено владельцу, он не возражал):** сейчас Шаг 1 = Customer & Dispatch, Шаг 2 = The Job. В звонке наоборот (что случилось → машина → цена → адрес → имя). Поменять местами: **Шаг 1 = The Job, Шаг 2 = Customer & Dispatch**; проверки телефона/имени/ZIP/адреса перенести из `nextStep` в кнопку «Create Job». Плюс: тип работы известен до выбора техника (TechPicker получает `jobType`). ⚠️ Это меняет поведение и для техников — показать владельцу отдельно.

### Шаг C. Проверка ✅
- `npx vitest run` (все тесты), `npx tsc --noEmit`.
- Превью: `npm run dev` (Vite :3000, API-прокси на :3001 → `npm run dev:server` нужен `DATABASE_URL` и т.п.). Если вход требует пароль — пароль не вводить за владельца; показать скриншоты через доступный режим или попросить владельца войти.
- Проверить: десктоп (панель справа, цены день/ночь, переключение скрипта при выборе шаблона, возражения), мобильный (кнопка + лист), техник панель не видит.

### Шаг D. Показать владельцу → одобрение → деплой ⏳
Процесс владельца: **сначала показать (скриншоты/превью), потом деплой.** Push в main делает владелец сам (даём одну команду). Не мержить без одобрения.
Скриншоты показаны 2026-09-18. После «да»: `git fetch origin`, убедиться, что `git log HEAD..origin/main` пуст (иначе rebase), затем из этого worktree `git push origin HEAD:main`. Локальный `main` в `C:\Clodecode\CRM` не трогать.

### Фаза 2 — сделано 2026-09-18 (владелец: «всё сейчас, запуск в конце»)
- ✅ Вкладка **Training / «Обучение»** (owner + manager): три документа из `training/`, тест-допуск и 10 учебных звонков.
  - Тест-допуск: 21 вопрос, нужно 100%. Цены берутся из живого прайса днём и ночью (`trainingQuiz.ts`), вопросы по зоне и 10 из 22 вопросов по правилам (`trainingContent.ts`).
  - Учебные звонки: у каждого роль «клиента», чек-лист и ловушки.
  - Владелец видит таблицу «Команда». Результаты лежат в `trainingResults` в settings по id пользователя.
- ✅ **Редактор фраз**: Settings → Call Scripts, только владелец. Предпросмотр, вставка цены, предупреждение по правилам честности (`honestyWarnings`). Правки лежат в `scriptOverrides`.
- ✅ **Причины незаписи**. Как отмечают:
  - крестик в New Job при начатой заявке открывает окно причины;
  - в Calls на новых входящих есть чип «Не записался?».

  Где видно: сводка за 7 дней в Calls и карточка в Marketing. Данные в журнале `lostCalls` в settings.
- ⏭ Авто-СМС на пропущенный — **не делаем**, у владельца уже есть в OpenPhone.
- ⏭ Сверка Sona — **не делаем**: с менеджерами Sona не планируют использовать.

### Фаза 3 — «свежий взгляд»: чтобы клиенту было приятно говорить (2026-09-18)
Владелец: «чтобы менеджеры были на высшем уровне, а клиенты получали удовольствие от разговора». Разобрано по исследованию (`research/01`) и принципам продаж/психологии.
- **Имя клиента — до цены** (шаг «Где и кто» во всех срочных скриптах), дальше по имени 2–3 раза. Панель сама подставляет имя клиента, техника и адрес, как только они введены в форму (голубым).
- **Прощание** последним шагом каждого скрипта: спасибо → имя → что дальше (+ живая деталь: тень в жару, «Enjoy the new place!»). Правило «пик и конец»: звонок помнят по концу. Для не записавшихся — тёплый шаг «Если не записывается».
- Приветствие «Thanks for calling TrustKey Locksmith, this is …»; повтор «I'm sending [Tech]»; ночью цена сама говорит «— our after-8PM rate» (токен `{night}`).
- **Каждый ответ на возражение заканчивается вопросом вперёд** (обычно к адресу); подсказка начинается с того, что клиента на самом деле волнует. Новые ответы: злой клиент, «не знаю ответа», «откройте дверь моему жильцу».
- В панели раздел **«Как звучать»** (`DELIVERY_TIPS`): улыбка, темп, пауза после цены, «I», холд. В «Нельзя говорить» — фразы тона (Calm down, Hold on…, We'll try…, Do you want us to come out?).
- Тест-допуск: +5 вопросов (злой клиент, холд, прощание, имя, ночной тариф) — теперь 21 по правилам.
- **Разбор звонка** (вкладка в Обучении, owner + manager): оценка записи из OpenPhone по 12 пунктам (`scorecard.ts`), ★ — критичные, зачёт от 85% без провала ★. Хранится в `callReviews` (settings, сервер объединяет по id, техникам и складу не отдаётся). В «Команде» — средний балл последних 5 разборов и «тренировать: …» (самый слабый пункт). В «Учебных звонках» — те же 12 пунктов сверху.

### Фаза 4 — обучение для всех и «О компании» (2026-09-18)
Владелец: обучение открыто всем; менеджерам обязательно (тест 100%, работа по скриптам), остальным — по желанию, чтобы познакомиться с компанией.
- Вкладка **Обучение есть у всех ролей** (`visibleTabsFor`). Телефон (owner, manager): О компании → база знаний → скрипты → нельзя говорить → тест-допуск → учебные звонки → разбор звонка → команда (owner). Остальные: О компании → нельзя говорить → база знаний → скрипты → тест (по желанию, без «допуска» и без жёлтых предупреждений).
- **«О компании»** — `training/00-about.md` и `00-about.en.md`: кто мы, шесть обещаний клиенту (цена заранее/оплата после, правда даже если теряем заказ, защищаем клиента, бережно, держим слово, отвечаем за работу), **цена с телефона — это наша цена** (техник один раз объясняет; клиент держится за цену — делаем за неё без спора; разницу потом решаем с владельцем), **где конфликт — нас нет** (не выселяем жильцов, не встаём между людьми, техник вправе развернуться), правила, которые не нарушаем. Кнопка «Прочитал(а)» → `aboutReadAt`.
- **Два языка:** переключатель RU/EN (localStorage `training.lang`, по умолчанию RU). По-английски пока только «О компании» и подписи вкладок; остальные разделы честно пишут «only in Russian for now». Перевод остального — когда появится первый англоязычный сотрудник (скрипты ещё будут меняться, два текста разъедутся).
- **Сервер:** `PUT /api/settings/training` — любой роли, пишет только свою запись (кто — из токена, не из тела), мусор отклоняется (400). Техник и кладовщик получают в GET только свою запись. Клиент сохраняет результаты обучения только через этот адрес (раньше техник получил бы 403).
- «Команда»: все сотрудники, группы «Телефон — обязательно» и «Остальные — по желанию», колонка «О компании».
- Тест: +1 вопрос «цена с телефона — это наша цена» (22 по правилам). В `02-call-scripts.md` — строка об этом в формуле цены.
- Тур `tab-training-team` для техника/бухгалтера/кладовщика; тур офиса начинается с «О компании».
- Документы: таблицы из двух колонок на телефоне переносятся, а не листаются вбок (`Markdown.tsx`).

---

## 4. Найденные проблемы (сообщить владельцу / отдельные задачи)

1. ~~**Прайс в `priceBook.ts` на origin/main устарел**~~ — **исправлено в этой ветке** (2026-09-18). Ночные цены больше не хранятся, их считает `nightPriceOf()` (цена + $60), поэтому старые $269 у commercial ни на что не влияют.

   Живой прайс переводится на `PRICE_BOOK_VERSION = 2` один раз:
   - ключ `r-car-key` $99 → «no chip» $149;
   - добавляется `r-key-extraction` $169;
   - убирается `r-fob-program`.

   Как это работает:
   - запускается у первого владельца или менеджера после выкладки, только на свежих данных с сервера, и трогает только ставки со старыми сидовыми ценами;
   - если на сервере прайса ещё нет, уходит полный список, а не две ставки;
   - контракт с сервером проверен тестом `server/routes/settings.priceBook.test.js`.

   Незакоммиченный фикс в `C:\Clodecode\CRM` больше не нужен.
2. **Название компании:** в `settingsStore` дефолт `companyName: 'Salem Locksmith'`. Скрипт приветствия написан с «TrustKey Locksmith» жёстко (`COMPANY` в `callScripts.ts`), потому что все тексты — про TrustKey/Аризону. Если CRM станет мультикомпанийной — перевести на настройку компании.
3. **Сайт** всё ещё предлагает «Key fob programming (customer-supplied) $99» в `pricing.md` / `llms.txt` — отдельная задача в репо сайта.

## 5. Юридические рамки (Аризона) — учитывать в скриптах

- **Лицензии локсмита в Аризоне не существует** → честный ответ на «Are you licensed?»: «Arizona doesn't have a state locksmith license. We're a registered Arizona company and we carry liability insurance.» Никогда «licensed / bonded / ROC / ALOA member».
- **Установка замков = работа подрядчика.** Без ROC — только по handyman-исключению ARS 32-1121(A)(14): проект **< $1,000 всего** (работа + материалы), дробить нельзя, без permit, и **в рекламе должна быть фраза «Not a licensed contractor»**.
- **Panic bars / exit devices — нельзя вообще** без ROC (противопожарные работы, § 32-1121(D)). Менеджер такие заказы не принимает → владельцу.
- **Запись звонков:** Аризона — согласие одной стороны (ARS 13-3005/3012); для звонящих из CA/FL/WA и др. — включить в OpenPhone объявление «This call may be recorded».
- **Найм менеджеров:** если это сотрудники в Аризоне — workers' comp обязателен с первого работника (§ 23-961). Уточнить у владельца (штат / подрядчики / где находятся).

## 6. Правила честности (коротко; полный список — `training/03-never-say.md`)

Нельзя: licensed; любые «N минут» наугад и «среднее время приезда»; гарантия больше **30 дней** (только на наши замки и ключи); background-checked; «команды по всей долине»; best/#1/top-rated/отзывы/звёзды; «с 2010 года»; сравнения с дилером в процентах; ругать конкурентов; «никогда не сверлим» / «не открыли — бесплатно»; письменная смета до выезда (только коммерция после осмотра); точная цена автоключа по телефону (только «from», подтверждение на месте, $59 service call засчитывается); скидки.
Можно: insured; locally owned, based in Mesa; our own techs, never subcontractors; flat price quoted before dispatch (вскрытие / rekey / установка); payment after the job; СМС с именем и фото техника; машина с логотипами, техник в форме; 30-day warranty; $20 off через форму на сайте.
